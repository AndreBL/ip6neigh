#UCI example configuration file for ip6neigh script

config ip6neigh 'config'

	# Logical interface name. Default: 'lan'
	# option interface	'lan'

	# When a new neighbor is discovered, auto probe for other addresses based
	# on MAC.
	# 0=disabled, 1=all scopes, 2=same scope. Default: '1'
	#option probe_eui64	'1'

	# When a new neighbor is discovered, auto probe for other addresses with
	# the same interface identifier. 
	# 0=disabled, 1=enabled. Default: '1'
	#option probe_iid	'1'

	# Label for link-local addresses. Default: 'LL'
	#option ll_label	'LL'

	# Label for unique local addresses (ULA). Default is no label for ULAs.
	#option ula_label	'ULA'

	# Label for globally unique addresses (GUA).
	# Defaults to 'PUB' if network has ULA prefix and no label if network
	# network does not have ULA prefix.
	#option gua_label	'PUB'

	# Secondary label for temporary (RFC 4941) address. Will also be applied
	# to addresses with Semantically Opaque Interface Identifiers (RFC 7217).
	# Default: 'TMP'
	#option tmp_label	'TMP'

	# Name to be used for hosts that do not supply their name to DHCPv6 or
	# DHCPv4 servers. A six digit (hex) NIC identifier will be appended after
	# it. Use '0' if you want to disable this feature. Default: 'Unknown-'
	#option	unknown		'Unknown-'

	# Enable loading of predefined static or MAC-based IIDs from /etc/config/dhcp.
	# 0=disabled, 1=enabled. Default: '1'
	#option load_static	'1'

	# Enable logging of debug messages. Options are '0' for disabled,
	# '1' to use syslog or the file name if you want to log to a file instead.
	# Default: '0'
	option log		'/tmp/log/ip6neigh.log'
